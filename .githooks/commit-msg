#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
exec python3 "$repo_root/tools/check_commit_attribution.py" \
  --config "$repo_root/.github/attribution-policy.json" \
  --message-file "$1"
