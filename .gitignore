# macOS
.DS_Store

# Local tooling artifacts — never published
.gitnexus/
.claude/
graphify-out/
CLAUDE.md
.hermes.md
# gitnexus auto-generates AGENTS.md with internal tool references —
# ignore until a clean public version is approved
AGENTS.md
