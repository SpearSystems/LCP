# LCP Extension Registry

> Placeholder — namespace registration format to be finalized before
> public publication. Modeled on the LEX extension registry: accepts all
> namespace registrations following `{org}.{division}.{purpose}`.
> Rejection grounds only: collision with `lcp.core.*`, demonstrably
> malicious content, or format violation. No editorial veto, no fees.
